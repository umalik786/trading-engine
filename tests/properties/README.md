# Property tests

These tests are hand-written by the operator, deliberately — see spec §7.3.

Do not generate these tests from the specification. A test generated from the
same specification as the implementation only confirms the two agree with
each other, not that either is correct. If the spec was misunderstood, a
generated test would encode that same misunderstanding and pass against the
bug it should have caught.
